apiVersion: v1
kind: Service
metadata:
  name: ${API_APP}
  namespace: ${NAMESPACE}
  labels:
    app: ${API_APP}
spec:
  type: ClusterIP
  selector:
    app: ${API_APP}
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
