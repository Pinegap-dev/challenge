apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${WEB_APP}
  namespace: ${NAMESPACE}
  labels:
    app: ${WEB_APP}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ${WEB_APP}
  template:
    metadata:
      labels:
        app: ${WEB_APP}
    spec:
      containers:
        - name: web
          image: ${WEB_IMAGE_URI}
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
